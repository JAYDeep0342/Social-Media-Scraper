"""Reserved for future cross-cutting abstract contracts (Protocols/ABCs).

Providers, ranking, and pipeline currently define their own base classes
directly in their own packages (see app.providers.base, app.ranking.base,
app.pipeline.base); this package is for contracts shared across more than
one future package (e.g. extractors/parsers) once those are implemented.
"""
